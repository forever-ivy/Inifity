# Translation Desktop App - 需求规划 (最终版)

## 1. 产品定位

### 前端职责
- ✅ 保证系统能稳定启动和常驻
- ✅ 让你看得清（状态、日志、错误、进度）
- ✅ 让你改得对（配置可视化与校验）
- ✅ 让你审得快（_VERIFY 产物浏览和质检报告可读）

### 前端不做
- ❌ 不做 new/run/status/ok/no/rerun 指令按钮
- ❌ 不抢 Telegram 作为主交互入口

> **原则**：现有协议和消息流保持 Telegram 主导，App 是运维仪表盘。

---

## 2. 技术栈

| 层级 | 技术 |
|-----|------|
| 前端 | React 18 + TypeScript |
| 样式 | TailwindCSS |
| 状态 | Zustand |
| 桌面 | Tauri 2.x (Rust) |
| 业务 | Python (现有，不改) |

---

## 3. 功能模块

### 3.1 启动与服务管控中心 【必做】

**目标**：双击即跑、出错可恢复

| 功能 | 说明 |
|-----|------|
| 一键启动服务 | 后台拉起轮询脚本 |
| 一键停止/重启 | 全量控制 |
| 服务健康状态灯 | Running / Degraded / Stopped |
| 自动重启策略 | 崩溃后 N 秒重启，最多 M 次 |
| 开机自启 | 可选 |
| 最小化到托盘 | 继续运行 |

### 3.2 环境预检与一键修复 【必做】

**目标**：防止"点了没反应"

**预检项**：
- [ ] Python / venv
- [ ] requirements 完整性
- [ ] .env.v4.local 存在与关键项完整
- [ ] OpenClaw 可用性
- [ ] LibreOffice（可选）
- [ ] Gemini Key（可选）

**预检分级**：
- 🔴 Blocker（不能启动）
- 🟡 Warning（可降级运行）
- 🟢 Info（建议优化）

**一键修复**：
- 创建 venv
- 安装依赖
- 生成/更新配置模板

### 3.3 配置中心 【必做】

**目标**：不再手改 env 文本

**可视化编辑 .env.v4.local**：
- 路径类：V4_WORK_ROOT, V4_KB_ROOT
- 路由/模型/RAG 参数
- IMAP 参数

**特性**：
- 字段级校验（路径存在、端口、URL、必填项）
- 配置版本快照（保存、回滚、差异对比）
- "生效提示"：哪些改动需重启服务

### 3.4 任务观测面板 【必做】

**目标**：你不发指令，但要看得懂全流程状态

**展示内容**：
- 活跃 job 列表（按 sender / channel / 时间）
- 状态流展示（collecting → running → review_ready / needs_attention）
- 里程碑时间线

**里程碑追踪**：
```
run_accepted
  → kb_sync_started → kb_sync_done
  → kb_retrieve_done
  → intent_classified
  → round_1_done → round_2_done → round_3_done
  → review_ready / needs_attention
```

### 3.5 产物与审核中心 【必做】

**目标**：加速人工验收

**按 job 浏览 _VERIFY/{job_id}**：

| 产物类型 | 展示方式 |
|---------|---------|
| Final.docx | 文件卡片 |
| Final-Reflow.docx | 文件卡片 |
| Review Brief.docx | 可读化渲染 |
| Change Log.md | Markdown 渲染 |
| Final.xlsx | 表格预览 |
| quality_report.json | 结构化视图 |
| openclaw_result.json | 结构化视图 |

**操作**：
- 一键打开本地文件
- 复制路径
- 标注"已人工审核"

### 3.6 日志与诊断中心 【必做】

**目标**：故障能定位、能交接、能复现

**功能**：
- 实时日志流（.system/logs/*）
- 筛选：job_id、时间、级别、关键词
- 错误聚合卡（最近 1h/24h top errors）
- 一键导出诊断包（脱敏日志 + 配置摘要 + 版本）
- 常见故障指引（从 README Troubleshooting 映射）

### 3.7 KB/RAG 健康页 【建议】

**目标**：跑前可知检索链是否可用

**内容**：
- ClawRAG 连接状态
- 最近同步时间与结果
- 最近检索命中统计
- 失败重试入口（仅健康检查/同步）

### 3.8 通知中心 【建议】

**目标**：Telegram 之外的本地提醒

**内容**：
- 本机通知：服务异常、任务 ready、需要人工关注
- 静默时段设置
- 通知历史列表

### 3.9 系统托盘 【建议】

**目标**：前台轻、后台稳

**托盘菜单**：
```
● Running
─────────────────
📊 打开 Dashboard
📁 打开 Verify 目录
📋 打开日志
─────────────────
🔄 重启服务
❌ 退出
```

### 3.10 安全与隐私 【建议】

**内容**：
- 敏感字段本地加密存储
- 日志脱敏（邮箱、手机号、token）
- 操作审计（本地）
- 导出诊断包自动脱敏

---

## 4. 页面信息架构 (IA)

```
┌─────────────────────────────────────────────────────────────┐
│  📊 Dashboard    🖥️ Services    📋 Jobs    📁 Verify       │
│  📋 Logs         ⚙️ Settings    [KB Health]                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                        [页面内容区]                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 页面分工

| 页面 | 核心内容 |
|-----|---------|
| **Dashboard** | 系统状态总览、服务状态灯、最近任务 |
| **Services** | 进程控制、预检结果、一键修复 |
| **Jobs** | 只读任务状态、里程碑时间线 |
| **Verify** | 产物浏览、质检报告、审核标记 |
| **Logs** | 实时日志、错误聚合、诊断导出 |
| **Settings** | 配置编辑、校验、快照管理 |
| **KB Health** | RAG 连接、同步状态（可选） |

---

## 5. 界面草图

### Dashboard
```
┌─────────────────────────────────────────────────────────────┐
│  Dashboard                                                   │
├─────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐ │
│  │  System Status                          ● All Running  │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐               │ │
│  │  │ Telegram │ │  Worker  │ │   RAG    │               │ │
│  │  │    ●     │ │    ●     │ │    ●     │               │ │
│  │  │ Running  │ │ Running  │ │ Healthy  │               │ │
│  │  └──────────┘ └──────────┘ └──────────┘               │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  Recent Jobs                          [View All →]          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ job_eventranz_0217  review_ready   10:32 AM           │ │
│  │ job_test_0216       needs_attention  Yesterday        │ │
│  │ job_sample_0215     verified         2 days ago       │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  Quick Actions                                              │
│  [🔄 Restart All]  [📁 Open Verify]  [📋 Export Diagnostics]│
└─────────────────────────────────────────────────────────────┘
```

### Services
```
┌─────────────────────────────────────────────────────────────┐
│  Services                                                    │
├─────────────────────────────────────────────────────────────┤
│  Pre-flight Check                                           │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 🟢 Python 3.11      🟢 venv          🟢 requirements  │ │
│  │ 🟢 .env.v4.local    🟢 OpenClaw      🟡 LibreOffice   │ │
│  │ [Run Pre-flight Check]                [Auto Fix All]   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  Service Control                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  ● Telegram Bot          Running (PID: 12345)          │ │
│  │    Uptime: 2h 34m  |  Restarts: 0  |  [Stop] [Logs]    │ │
│  │                                                        │ │
│  │  ● Run Worker            Running (PID: 12346)          │ │
│  │    Uptime: 2h 34m  |  Restarts: 0  |  [Stop] [Logs]    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  [▶ Start All]  [■ Stop All]  [🔄 Restart All]             │
│                                                             │
│  Auto-restart: [✓] Enable   Delay: [5]s   Max: [3] times   │
└─────────────────────────────────────────────────────────────┘
```

### Verify (产物审核)
```
┌─────────────────────────────────────────────────────────────┐
│  Verify                                                      │
├─────────────────────────────────────────────────────────────┤
│  Jobs awaiting review: 2                                    │
│                                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ job_eventranz_0217                          review_ready│ │
│  │ ───────────────────────────────────────────────────────│ │
│  │ 📄 Final.docx           [Open] [Copy Path]             │ │
│  │ 📄 Final-Reflow.docx    [Open] [Copy Path]             │ │
│  │ 📋 Review Brief.docx    [View]                         │ │
│  │ 📝 Change Log.md        [View]                         │ │
│  │ ───────────────────────────────────────────────────────│ │
│  │ 📊 Quality Report                                       │ │
│  │   • Terminology Hit: 87%  ✓                            │ │
│  │   • Structure Fidelity: 95% ✓                          │ │
│  │   • Purity Score: 99.2%   ✓                            │ │
│  │ ───────────────────────────────────────────────────────│ │
│  │ [Mark as Reviewed]  [Open in Finder]                   │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 开发计划

### Phase 1: 基础框架 (2天)
- [ ] 初始化 Tauri + React + TailwindCSS
- [ ] 路由和页面布局
- [ ] 系统托盘图标
- [ ] Zustand store 结构

### Phase 2: Services + 预检 (2天)
- [ ] 服务启动/停止/重启
- [ ] 预检逻辑
- [ ] 一键修复
- [ ] 服务状态监控

### Phase 3: Jobs + Verify (2天)
- [ ] 任务列表查询
- [ ] 里程碑时间线
- [ ] 产物浏览
- [ ] 质检报告渲染

### Phase 4: Logs + Settings (2天)
- [ ] 实时日志流
- [ ] 日志过滤
- [ ] 配置编辑器
- [ ] 校验和快照

### Phase 5: 打包发布 (1天)
- [ ] DMG 打包
- [ ] 测试
- [ ] 文档

**总计: 约 9 天**

---

## 7. Rust Commands API

```rust
// === 服务管理 ===
#[tauri::command] fn start_all_services() -> Result<Vec<ServiceStatus>, String>
#[tauri::command] fn stop_all_services() -> Result<(), String>
#[tauri::command] fn restart_all_services() -> Result<(), String>
#[tauri::command] fn get_service_status() -> Vec<ServiceStatus>

// === 预检 ===
#[tauri::command] fn run_preflight_check() -> PreflightResult
#[tauri::command] fn auto_fix_issues(issues: Vec<String>) -> Result<(), String>

// === 任务查询 ===
#[tauri::command] fn get_jobs(status: Option<String>, limit: u32) -> Vec<JobInfo>
#[tauri::command] fn get_job_detail(job_id: String) -> JobDetail
#[tauri::command] fn get_job_milestones(job_id: String) -> Vec<Milestone>

// === 产物 ===
#[tauri::command] fn list_verify_artifacts(job_id: String) -> Vec<Artifact>
#[tauri::command] fn read_artifact(path: String) -> Result<String, String>
#[tauri::command] fn open_in_finder(path: String) -> Result<(), String>

// === 日志 ===
#[tauri::command] fn read_logs(service: String, lines: u32) -> Vec<LogLine>
#[tauri::command] fn export_diagnostics() -> Result<String, String>

// === 配置 ===
#[tauri::command] fn get_config() -> AppConfig
#[tauri::command] fn save_config(config: AppConfig) -> Result<(), String>
#[tauri::command] fn validate_config(config: AppConfig) -> ValidationResult

// === KB/RAG ===
#[tauri::command] fn check_rag_health() -> RAGHealthStatus
#[tauri::command] fn trigger_kb_sync() -> Result<KBSyncResult, String>
```

---

*版本: v2.0*
*更新: 2026-02-17*
